{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "init": "",
    "reset": "rm -rf ./bun.lockb && rm -rf ./node_modules && rm -rf ./packages/*/node_modules && rm -rf ./apps/*/node_modules",
    "workspace": ""
  },
  "bin": "bin/monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "bun": "latest",
    "bun-types": "latest",
    "typescript": "latest"
  },
  "devDependencies": {},
  "peerDependencies": {}
}